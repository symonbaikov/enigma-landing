# [Чернетка 2.4 · uk] Від ключових слів до prompt/query clusters

> Шаблон: `ArticlePage` (slug `research-lab/prompt-query-clusters`). Кластер 2 · Технічні руководства. Статус: на ревʼю.
> Attribution: 61, 28, 15, 30, 64. Факти — лише з `docs/research.md`. Lint: бенчмарки = датасети/IR, не AEO-гайд; кластеризація = практична стратегія, не доведена формула.

---

## 1. Hero (H1)

**H1:** Від ключових слів до prompt/query clusters

**Підзаголовок:** Таргетувати треба групи питань та intent, а не окремі ключі. Це практична стратегія, виведена з SEO- й IR-досліджень, а не доведена формула.

**[CTA]** `Повернутися до хабу Research Lab`

---

## 2. Контекст / Проблема

- Один ключ ≠ один intent: користувачі формулюють запити по-різному.
- Перевірка однієї фрази не репрезентує весь кластер питань.
- Без кластерів неможливо системно міряти покриття intent у AI-відповідях.

---

## 3. Ядро

### 3.1. Чому кластер, а не ключ

Стратегія запитів — довгострокова. Erdmann et al. досліджують довгострокову стратегію вибору ключових слів у SEO та її вплив на конкурентність і стійкість трафіку [Джерело 61]. Для AEO/GEO це переноситься так: оптимізуємось під групи питань та intent (prompt/query clusters), а не під окремі ключі — це практична стратегія, не гарантія результату.

### 3.2. Користувачі питають питаннями

AEO виросло з QA-логіки. Natural Questions побудований на реальних питаннях користувачів і Wikipedia-документах [Джерело 30]. Висновок для кластерів: формулюйте елементи кластера як природні питання («як…», «що краще…», «X проти Y»), бо системи шукають фрагмент, що дає прямий відповідь.

### 3.3. Тестувати треба на різних типах запитів

Якість retrieval не можна міряти на одній групі ключів. BEIR — бенчмарк для оцінки retrieval-моделей на різних задачах і доменах; він показує, що retrieval-якість не зводиться до одного набору даних [Джерело 28]. Для GEO-аудиту: кластер має покривати різні типи intent, а не одну вузьку групу.

### 3.4. Типи елементів у кластері

| Тип запиту | Приклад intent | Навіщо в кластері |
|------------|----------------|-------------------|
| Категорійні | «найкращий інструмент для…» | Високий intent, висока ставка |
| Проблемні | «як вирішити…» | Ловить рішення-шукачів до назви категорії |
| Порівняльні | «X проти Y», «альтернативи Y» | Показує позиціонування відносно ринку |
| Брендові | прямі запити про бренд | Контроль точності й тональності, не лише покриття |

### 3.5. Чому клік перестав бути прямим сигналом

ORCAS описує великий набір агрегованих query-document click pairs для аналізу пошуку й показує роль кліків в навчанні та оцінці класичних систем [Джерело 64]. У генеративному пошуку кліки по документах зменшуються, а feedback стає менш прямим — тому кластер вимірюють по присутності у відповіді, а не лише по кліках.

### 3.6. Де AEO дає максимум

LLM-відповіді корисні не скрізь однаково. Caramancion досліджує, коли користувачі віддають перевагу LLM-відповідям, а коли класичному пошуку, у різних сценаріях інформаційного пошуку [Джерело 15]. Висновок для пріоритезації кластерів: найбільший ефект AEO — на складних, порівняльних і пояснювальних питаннях, де користувач хоче готову відповідь.

---

## 4. Блок E-E-A-T

| Джерело | Автори, рік, площадка | Що встановлює | Лінк |
|---------|------------------------|----------------|------|
| 61 | Erdmann et al., 2022, J. of Business Research | Довгострокова стратегія вибору ключів | [↗](https://www.sciencedirect.com/science/article/abs/pii/S0148296322000777) |
| 28 | Thakur et al., 2021, NeurIPS D&B | BEIR: retrieval-якість на різних задачах/доменах | [↗](https://arxiv.org/abs/2104.08663) |
| 15 | Caramancion, 2024, arXiv | Коли користувачі обирають LLM проти пошуку | [↗](https://arxiv.org/abs/2401.05761) |
| 30 | Kwiatkowski et al., 2019, TACL / Google | Natural Questions: реальні питання користувачів | [↗](https://research.google/pubs/natural-questions-a-benchmark-for-question-answering-research/) |
| 64 | Craswell et al., 2020, CIKM / arXiv | ORCAS: роль кліків у навчанні/оцінці пошуку | [↗](https://arxiv.org/abs/2006.05324) |

---

## 5. AEO / FAQ

### Що таке prompt/query cluster?
Група запитів і формулювань, що відображають один intent (наприклад, «найкращий X», «X проти Y», «як зробити X»). Таргетують кластер, а не окремий ключ — це практична стратегія [Джерело 61].

### Чому не можна перевіряти одну фразу?
Бо одна фраза не репрезентує intent-кластер, а retrieval-якість залежить від типу запиту й домену [Джерело 28]. Кластер має покривати різні типи intent.

### Як формулювати елементи кластера?
Як природні питання користувачів — answer engines шукають фрагмент, що дає прямий відповідь; QA-датасети будуються саме на реальних питаннях [Джерело 30].

### Де AEO дає найбільший ефект?
На складних, порівняльних і пояснювальних запитах, де користувач хоче готову відповідь, а не список посилань [Джерело 15].

---

## 6. Schema (плейсхолдери)

> Псевдокод-плейсхолдери для ревʼю. У production — валідний JSON-LD без коментарів. **Увага:** FAQ rich results вилучені з Google Search із 07.05.2026 — `FAQPage` лише як опціональна семантична розмітка (не Google rich-result lever); основна схема — `Article`. Видимий FAQ-контент зберігаємо. **У коді:** `Article` завжди; видимий FAQ завжди; `FAQPage` — опціонально / за feature-flag; коментарі `//` та `/* */` видалити, `mainEntity` заповнити.

```jsonld
// Article
{ "@context":"https://schema.org", "@type":"Article",
  "headline":"Від ключових слів до prompt/query clusters",
  "about":["query clusters","intent","AEO strategy"],
  "dateModified":"{{UPDATED_AT}}" }
```
