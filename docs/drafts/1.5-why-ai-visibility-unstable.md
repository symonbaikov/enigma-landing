# [Чернетка 1.5 · uk] Чому AI-видимість нестабільна

> Шаблон: `ChapterPage` (slug `geo-playbook/why-visibility-unstable`, num `05`). Статус: на ревʼю.
> Attribution: 06, 20, 36, 44, 45. Факти — лише з `docs/research.md`.

---

## 1. Hero (H1)

**H1:** Чому AI-видимість нестабільна — і як її вимірювати правильно

**Підзаголовок:** Відповіді AI і набір цитованих джерел змінюються між запусками. Один замір дає хибну картину. Цей розділ задає логіку повторюваного вимірювання.

**[CTA]** `Перейти до Розділу 6: технічне SEO як фундамент GEO`

---

## 2. Контекст / Проблема

- Перевірка одного промпту один раз ≠ ваша позиція в AI-пошуку.
- Той самий запит у різні дні дає різні відповіді й різні джерела.
- Без повторюваних замірів неможливо відрізнити сигнал від шуму.

---

## 3. Ядро

### 3.1. Один замір вводить в оману

Видимість у AI-пошуку — імовірнісна, не фіксована. Schulte показує, що відповіді AI-пошуковиків і набори цитованих джерел нестабільні між запусками, тож один замір може давати хибне уявлення про позицію бренду [Джерело 06]. Висновок прямий: моніторинг — це повторюваний замір, а не разова перевірка промпту.

### 3.2. Чому зник прямий зворотний звʼязок

У класичному web search кліки й dwell time можуть використовуватись як fine-grained feedback для покращення ranking-моделей; у генеративному пошуку зворотний звʼязок часто привʼязаний до фінальної відповіді, тому його важче мапити на етапи retrieval/generation. Dai et al. пропонують повернути fine-grained feedback на етапи decomposition, retrieval і generation [Джерело 20]. Тому видимість залежить не тільки від документа, а й від поведінки на етапах AI-пошуку.

### 3.3. Що саме вимірювати

Якщо результат нестабільний, метрика має бути розподілом, а не точкою.

| Параметр заміру | Чому потрібен |
|------------------|----------------|
| Кілька прогонів одного промпту | Відповіді змінюються між запусками [06] |
| Кілька днів / часові вікна | Видимість дрейфує в часі [06] |
| Кілька платформ | Набори джерел відрізняються між системами |
| Набір промптів, не один | Один запит не репрезентує intent-кластер |

### 3.4. Метрики з RAG-evaluation

Оцінювати треба не лише факт згадки, а й якість використання джерела. Огляд оцінювання RAG дає набір вимірів: релевантність, точність, підтримка тверджень, стійкість [Джерело 36]. RAGAS формалізує метрики faithfulness, answer relevancy і context relevance для RAG-пайплайнів [Джерело 44]. Їх можна адаптувати для GEO-дашборда, але це не готовий стандарт: dashboard має окремо фіксувати platform, prompt set, run count і citation/absorption.

### 3.5. Один контент — різні системи

Та сама сторінка по-різному поводиться в різних продуктах. Бенчмарк Chen et al. показує, що один і той самий контекст моделі використовують неоднаково: точність і помилки відрізняються між моделями [Джерело 45]. Тож share of voice спершу рахуємо окремо для кожної платформи (platform-level breakdown); агрегований показник допустимий лише як вторинний шар поверх цього розбиття, а не замість нього.

---

## 4. Блок E-E-A-T

| Джерело | Автори, рік, площадка | Що встановлює | Лінк |
|---------|------------------------|----------------|------|
| 06 | Schulte, 2026, arXiv | Відповіді й цитати нестабільні між запусками | [↗](https://arxiv.org/abs/2604.07585) |
| 20 | Dai et al., 2025, SIGIR | Зворотний звʼязок у генеративному пошуку ослаб | [↗](https://arxiv.org/abs/2505.14680) |
| 36 | Yu et al., 2024, arXiv | Набір вимірів для оцінювання RAG | [↗](https://arxiv.org/abs/2405.07437) |
| 44 | Es et al., 2024, EACL Demo | RAGAS: faithfulness, relevancy, context relevance | [↗](https://aclanthology.org/2024.eacl-demo.16/) |
| 45 | Chen et al., 2024, AAAI | Моделі по-різному використовують той самий контекст | [↗](https://arxiv.org/abs/2309.01431) |

---

## 5. AEO / FAQ

### Чому відповідь AI про мій бренд щоразу інша?
Бо відповіді й набір цитованих джерел нестабільні між запусками — це властивість генеративного пошуку, а не помилка [Джерело 06].

### Скільки разів треба перевіряти видимість?
Багаторазово: кілька прогонів одного промпту, кілька днів і кілька платформ. Один замір методологічно слабкий і може давати хибну картину; статистичну надійність задають через протокол повторів (кількість прогонів, вікна, дисперсія) [Джерело 06].

### Чому не можна усереднити видимість «по всіх AI»?
Бо різні системи по-різному використовують той самий контент; точність і набір джерел відрізняються між моделями [Джерело 45]. Share of voice рахують окремо для кожної платформи.

### Які метрики брати для GEO-дашборда?
Faithfulness, answer relevancy і context relevance з RAG-evaluation — адаптовані, не як готовий стандарт; рахувати на повторюваних замірах і фіксувати platform, prompt set, run count, citation/absorption [Джерело 36, 44].

---

## 6. Schema (плейсхолдери)

> Псевдокод-плейсхолдери для ревʼю. У production — валідний JSON-LD без коментарів. **Увага:** FAQ rich results вилучені з Google Search із 07.05.2026 — `FAQPage` лише як опціональна семантична розмітка (не Google rich-result lever); основна схема — `Article`. Видимий FAQ-контент зберігаємо. **У коді:** `Article` завжди; видимий FAQ завжди; `FAQPage` — опціонально / за feature-flag; коментарі `//` та `/* */` видалити, `mainEntity` заповнити.

```jsonld
// Article
{ "@context":"https://schema.org", "@type":"Article",
  "headline":"Чому AI-видимість нестабільна — і як її вимірювати правильно",
  "isPartOf":{"@type":"Book","name":"GEO Playbook"},
  "about":["AI search visibility","measurement","RAG evaluation"],
  "citation":[
    {"@type":"CreativeWork","name":"Don't Measure Once: Measuring Visibility in AI Search","url":"https://arxiv.org/abs/2604.07585"},
    {"@type":"CreativeWork","name":"RAGAS","url":"https://aclanthology.org/2024.eacl-demo.16/"}
  ],
  "dateModified":"{{UPDATED_AT}}" }
```
```jsonld
// FAQPage — мапиться з блоку 5 (4 Q&A)
{ "@context":"https://schema.org", "@type":"FAQPage", "mainEntity":[ /* блок 5 */ ] }
```
