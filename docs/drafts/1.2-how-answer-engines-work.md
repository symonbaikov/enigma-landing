# [Чернетка 1.2 · uk] Як працюють answer engines: RAG і retrieval

> Шаблон: `ChapterPage` (slug `geo-playbook/how-answer-engines-work`, num `02`). Статус: на ревʼю.
> Attribution: 24, 25, 37, 38, 48, 18, 14. Факти — лише з `docs/research.md`.

---

## 1. Hero (H1)

**H1:** Як працюють answer engines: RAG і retrieval

**Підзаголовок:** Щоб бути джерелом відповіді AI, треба розуміти пайплайн «пошук → генерація». Цей розділ розбирає його по етапах і показує, де саме вирішується ваша видимість.

**[CTA]** `Перейти до Розділу 3: passage retrieval`

---

## 2. Контекст / Проблема

- «Оптимізувати під AI» без розуміння retrieval — це здогадки.
- У retrieval-enabled answer engines (RAG-пайплайнах) відповідь будується не з усього вебу, а з вузького набору вилучених документів; частина відповідей LLM може йти й із параметричної памʼяті.
- Якщо фрагмент не потрапив у retrieval-контекст конкретного answer-engine run, він не бере участі в генерації цієї відповіді.

---

## 3. Ядро

### 3.1. Базовий принцип RAG

Retrieval-Augmented Generation означає: у retrieval-enabled системах модель спершу вилучає документи, потім генерує відповідь з опорою на знайдені джерела. Lewis et al. формалізували цю схему як окрему архітектуру для knowledge-intensive задач [Джерело 24]. Практичний висновок для GEO (не гарантія, а технічна передумова): легка вилучуваність і чітка структура підвищують шанс потрапити в контекст відповіді, але не гарантують inclusion у конкретних комерційних answer engines.

### 3.2. Зовнішня памʼять у retrieval-augmented моделях

Retrieval — не надбудова, а частина того, як retrieval-augmented модель «знає». REALM показав, що мовна модель може використовувати зовнішній корпус знань уже на етапі переднавчання й під час відповіді [Джерело 25]. Це пояснює, чому вилучуваність і якість зовнішнього контенту стають частиною AI-видимості, а не косметикою поверх неї.

### 3.3. Етапи пайплайна

Сучасні answer engines — це конвеєр, а не один крок. Оглядові роботи описують типову архітектуру RAG: retrieval-метод, ранжування, генерація, оцінювання й практичні проблеми [Джерело 37], а свіжіший огляд розглядає retrieval як стандартну частину генеративних продуктів [Джерело 38].

| Етап | Що відбувається | Де вирішується видимість |
|------|------------------|---------------------------|
| Retrieval | Вилучення кандидатів-документів за запитом | Чи знайдено й чи вилучено вашу сторінку |
| Ranking / відбір | Відсів і впорядкування кандидатів | Чи пройшов фрагмент фільтри якості |
| Generation | Складання відповіді з опорою на контекст | Чи використано й чи процитовано джерело |
| Evaluation / feedback | Оцінка якості retrieval, відповіді й атрибуції | Може використовуватись для налаштування системи, але не гарантує майбутню видимість конкретного сайту |

### 3.4. Від пошуку до браузингу

AEO-логіка виросла з web-assisted QA. WebGPT навчав модель користуватися браузером, шукати джерела й відповідати з опорою на знайдене — ранній приклад LLM як answer engine [Джерело 48]. У retrieval-enabled answer engines відповідь часто поєднує знайдені джерела з параметричними знаннями моделі; retrieval підвищує grounding, але не усуває роль model memory.

### 3.5. Чому SEO-логіка не переноситься напряму

Інтерфейс став діалоговим, але під ним — retrieval, ranking і reasoning. Огляд Xiong et al. описує архітектурні, користувацькі й оцінювальні виклики на перетині пошукових сервісів і LLM [Джерело 14], а робота Ma et al. розкриває механізми, через які chat-based системи формують звʼязну відповідь [Джерело 18]. Висновок: контент треба робити зручним і для вилучення, і для генерації звʼязного тексту — це дві різні вимоги.

---

## 4. Блок E-E-A-T

| Джерело | Автори, рік, площадка | Що встановлює | Лінк |
|---------|------------------------|----------------|------|
| 24 | Lewis et al., 2020, NeurIPS | RAG: спершу retrieve, потім generate | [↗](https://arxiv.org/abs/2005.11401) |
| 25 | Guu et al., 2020, ICML | REALM: зовнішній корпус у переднавчанні й відповіді | [↗](https://arxiv.org/abs/2002.08909) |
| 37 | Gupta et al., 2024, arXiv | Системний огляд архітектур і проблем RAG | [↗](https://arxiv.org/abs/2410.12837) |
| 38 | Zhao et al., 2026, Springer | Retrieval як стандарт генеративних продуктів | [↗](https://link.springer.com/article/10.1007/s41019-025-00335-5) |
| 48 | Nakano et al., 2021/22, OpenAI | WebGPT: браузинг + QA з опорою на джерела | [↗](https://arxiv.org/abs/2112.09332) |
| 18 | Ma et al., 2024, arXiv | Механізми формування відповіді chat-search | [↗](https://arxiv.org/abs/2402.19421) |
| 14 | Xiong et al., 2024, arXiv | Виклики на перетині search-сервісів і LLM | [↗](https://arxiv.org/abs/2407.00128) |

---

## 5. AEO / FAQ

### Що таке RAG простими словами?
Retrieval-Augmented Generation — підхід, за якого модель спершу шукає й вилучає релевантні документи, а потім будує відповідь, поєднуючи знайдені джерела з параметричною памʼяттю моделі [Джерело 24].

### Чому мій контент не потрапляє у відповідь, хоча сторінка є в Google?
Бо у пайплайні answer engine є етап retrieval і відбору перед генерацією. Якщо фрагмент не вилучено або не пройшов фільтри якості, він не дійде до етапу генерації відповіді.

### Retrieval чи генерація важливіші для видимості?
Обидва. Спершу контент має бути вилучений (retrieval), потім — реально використаний у складанні відповіді (generation). Провал на retrieval або generation етапі може обнулити видимість у конкретній відповіді / конкретному прогоні, але не є постійним станом для всіх систем.

### Чому стара SEO-тактика не працює в AI-пошуку?
Бо під діалоговим інтерфейсом лишаються retrieval, ranking і reasoning з власними вимогами; контент має бути зручним і для вилучення, і для генерації звʼязної відповіді [Джерело 14, 18].

---

## 6. Schema (плейсхолдери)

> Псевдокод-плейсхолдери для ревʼю. У production — валідний JSON-LD без коментарів. **Увага:** FAQ rich results вилучені з Google Search із 07.05.2026 — `FAQPage` лише як опціональна семантична розмітка (не Google rich-result lever); основна схема — `Article`. Видимий FAQ-контент зберігаємо. **У коді:** `Article` завжди; видимий FAQ завжди; `FAQPage` — опціонально / за feature-flag; коментарі `//` та `/* */` видалити, `mainEntity` заповнити.

```jsonld
// Article
{ "@context":"https://schema.org", "@type":"Article",
  "headline":"Як працюють answer engines: RAG і retrieval",
  "isPartOf":{"@type":"Book","name":"GEO Playbook"},
  "about":["Retrieval-Augmented Generation","answer engines","information retrieval"],
  "citation":[
    {"@type":"CreativeWork","name":"Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks","url":"https://arxiv.org/abs/2005.11401"},
    {"@type":"CreativeWork","name":"WebGPT","url":"https://arxiv.org/abs/2112.09332"}
  ],
  "dateModified":"{{UPDATED_AT}}" }
```
```jsonld
// FAQPage — мапиться з блоку 5 (4 Q&A)
{ "@context":"https://schema.org", "@type":"FAQPage", "mainEntity":[ /* блок 5 */ ] }
```
